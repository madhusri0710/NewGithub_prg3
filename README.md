
2.	Create a Student table with the following fields:
o	StudentID – Number(5) – Primary Key
o	StudentName – Varchar(20)
o	DOB – Date
o	Gender – Varchar(10)
o	DepartmentID – Number(5) Add appropriate constraints such as PRIMARY KEY, UNIQUE, and NOT NULL.

	Alter the Student table by adding the following columns:
o	Email – Varchar(30)
o	PhoneNumber – Number(10) Then display the modified table structure.
